import { describe, it, expect } from "vitest";

describe("i18n string coverage", () => {
  it.todo(
    "no .tsx files in app/routes/ contain hardcoded English UI strings"
  );
  it.todo(
    "no .tsx files in app/components/ contain hardcoded English UI strings"
  );
  it.todo("no Intl calls use hardcoded 'en' locale");
});
