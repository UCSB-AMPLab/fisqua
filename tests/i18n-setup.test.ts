import { describe, it, expect } from "vitest";

describe("i18n setup", () => {
  it.todo("exports i18nextMiddleware, getLocale, and getInstance from middleware");
  it.todo("configures es as default language and en as fallback");
  it.todo("sets common as the default namespace");
  it.todo("includes initReactI18next in plugins");
});
