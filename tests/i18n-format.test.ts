import { describe, it, expect } from "vitest";

describe("es-CO formatting", () => {
  it.todo("relativeTime returns a dash for null input");
  it.todo(
    "relativeTime returns Spanish relative time string for a recent timestamp"
  );
  it.todo("formatDate returns day-de-month-de-year format");
  it.todo("formatNumber uses punto for thousands separator");
});
