import { describe, test } from "vitest";

describe("Reviewer approve/send-back (DESC-07)", () => {
  test.todo("reviewer can approve a described entry");

  test.todo("reviewer can send back an entry with comment");

  test.todo("sent-back entry returns to in-progress status");

  test.todo("cataloguer sees reviewer feedback on sent-back entry");

  test.todo("reviewer can edit description fields directly");
});
