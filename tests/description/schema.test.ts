import { describe, test } from "vitest";

describe("Description schema (DESC-01)", () => {
  test.todo("entries table has description status column");

  test.todo("entries table has all 11 description metadata columns");

  test.todo(
    "entries table has assignment columns (assignedDescriber, assignedDescriptionReviewer)"
  );

  test.todo("comments table exists with correct columns");

  test.todo("resegmentation_flags table exists with correct columns");
});
