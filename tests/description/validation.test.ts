import { describe, test } from "vitest";

describe("Description field validation (DESC-08)", () => {
  test.todo("submitForReview rejects when title is missing");

  test.todo("submitForReview rejects when resourceType is missing");

  test.todo("submitForReview rejects when dateExpression is missing");

  test.todo("submitForReview rejects when scopeContent is missing");

  test.todo("submitForReview rejects when language is missing");

  test.todo("submitForReview rejects when extent is missing");

  test.todo("submitForReview accepts when all required fields present");

  test.todo("validation returns field-level errors");
});
